# 静态代码分析器 — 核心框架逻辑与关键代码实现说明

---

## 1. 总体架构

分析器采用经典**编译器 Pass 管道**架构，每个分析阶段顺序执行并将结果传递给下一阶段：

\`\`\`
源代码 (C/C++/Java/Python)
     │
     ▼
┌─────────────────────────────────────────────────────┐
│  Phase 1: 语言感知解析器  →  统一 IR (中间表示)     │
│  PythonParser (ast模块) / GenericParser (正则)      │
└──────────────────────┬──────────────────────────────┘
                       │ IRNode 树
                       ▼
┌─────────────────────────────────────────────────────┐
│  Phase 2: 数据流分析引擎                            │
│  • IR 树遍历分析                                    │
│  → 检测未初始化变量、死赋值                         │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│  Phase 3: 污点分析引擎                              │
│  Source → Propagation → Sink                        │
│  → 检测注入类漏洞、缓冲区溢出、资源泄漏            │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│  Phase 4: 模式检测器                                │
│  语言特定的正则/AST模式匹配                         │
│  → 代码坏味道、安全反模式、CWE漏洞                 │
└──────────────────────┬──────────────────────────────┘
                       │ AnalysisResult (含所有 AnalysisIssue)
                       ▼
┌─────────────────────────────────────────────────────┐
│  Phase 5: 修复报告生成器 + GUI 渲染                 │
│  按类别聚合问题 + CWE 链接 + 修复建议               │
└─────────────────────────────────────────────────────┘

```
	代码坏味道意味着：代码的设计或编写方式存在问题。虽然代码现在能跑通，但它的结构很糟糕，会导致未来维护困难、容易产生潜在 Bug、或者难以扩展。
例如：
过长函数 (Long Method)：一个函数写了几百行。这意味着逻辑太复杂，应该拆分。

重复代码 (Duplicate Code)：同样的代码在多处出现。一旦要修改，必须改动所有地方，极易漏掉。

过大类 (Large Class)：一个类承担了太多的职责（违反了单一职责原则）。

依恋情节 (Feature Envy)：一个函数频繁调用另一个类的属性或方法，远超过对自身类调用的频率。这暗示该函数可能放错位置了。

魔术数字 (Magic Numbers)：代码中直接出现硬编码的数字（如 if (status == 5)），而没有定义为常量。别人很难理解 5 代表什么。

过长参数列 (Long Parameter List)：一个函数传入了 7、8 个参数。
```



## 2. 中间表示层 (IR) — `core/ir.py`

设计思想

为了实现**跨语言分析**，设计了一套与语言无关的统一 IR，将 C/C++/Java/Python 的 AST 节点映射到相同的 `IRNode` 类型集合。

### 核心数据结构

```python
@dataclass
class IRNode:
    node_type: NodeType      # 统一节点类型枚举（IF/WHILE/CALL/ASSIGN等40+种）
    value: Optional[str]     # 节点携带的值（变量名/函数名/字面量）
    line: int                # 源码行号（用于问题定位）
    children: List[IRNode]   # 子节点（构成 AST 树）
    taint: bool              # 污点标记（是否携带不可信数据）
    taint_sources: Set[str]  # 污点来源集合
    reaching_defs: Set[str]  # 可到达定义集合（数据流注解）
```

**NodeType 枚举** 覆盖了四种语言的公共控制流结构：

| 类别 | 节点类型 |
|------|---------|
| 控制流 | IF, ELSE, WHILE, FOR, FOREACH, RETURN, BREAK, TRY, CATCH |
| 声明 | VAR_DECL, PARAM_DECL, FUNCTION, CLASS_DECL |
| 表达式 | ASSIGN, CALL, BINARY_OP, UNARY_OP, MEMBER_ACCESS |
| 字面量 | IDENTIFIER, LITERAL, STRING_LITERAL, NULL |

### AnalysisResult 结构

```python
@dataclass
class AnalysisResult:
    language: Language
    issues: List[AnalysisIssue]       # 分析发现的所有问题
    ir_root: Optional[IRNode]         # IR 树根节点
    fix_report: str                   # 修复报告文本
    stats: Dict[str, int]             # 统计信息
    source_lines: List[str]           # 原始源代码行
```

---

## 3. 解析层 — `parsers/`

### Python 解析器（高精度）

利用 Python 标准库 `ast` 模块，直接获取精确的 AST：

```python
class PythonParser:
    def parse(self, code: str) -> AnalysisResult:
        try:
            tree = ast.parse(code)          # 标准库 AST
            ir_root = self._convert_node(tree)
        except SyntaxError as e:
            # 捕获语法错误并记录为 AnalysisIssue
            ...

    def _ast_to_ir(self, node: ast.AST) -> IRNode:
        if isinstance(node, ast.FunctionDef):
            return IRNode(NodeType.FUNCTION, value=node.name, line=node.lineno)
        elif isinstance(node, ast.Assign):
            targets = ast.unparse(node.targets)    # 提取赋值目标
            return IRNode(NodeType.ASSIGN, value=targets, line=node.lineno)
        elif isinstance(node, ast.Call):
            func = ast.unparse(node.func)          # 提取被调函数名
            return IRNode(NodeType.CALL, value=func, line=node.lineno)
        ...
```

**关键设计**：Python 解析器用 `ast.unparse()` 将子树还原为字符串，保留了足够的语义信息，同时保持 IR 层的简洁性。

### 通用解析器（正则驱动）

对 C/C++/Java 使用正则表达式进行模式匹配，无需外部依赖：

```python
class GenericParser:
    def _parse_line(self, line: str, lineno: int) -> IRNode:
        # 函数声明模式
        if re.search(r'\b\w[\w\s\*]*\s+\w+\s*\([^)]*\)\s*\{?', line):
            m = re.search(r'(\w+)\s*\(', line)
            return IRNode(NodeType.FUNCTION, value=m.group(1), line=lineno)
        # 变量声明
        if re.match(r'(int|long|float|double|String|auto)\s+', line):
            return IRNode(NodeType.VAR_DECL, ...)
        # 赋值语句
        if '=' in line and not line.startswith('if'):
            return IRNode(NodeType.ASSIGN, ...)
        ...

    def _check_braces(self, code: str, result: AnalysisResult):
        """括号匹配检测：O(n) 栈式扫描"""
        depth = 0
        for i, ch in enumerate(code):
            if ch == '{': depth += 1
            elif ch == '}': depth -= 1
            if depth < 0:
                # 多余的 '}'
                line = code[:i].count('\n') + 1
                result.add_issue(AnalysisIssue(..., cwe_id="CWE-691"))
                depth = 0
```

---

## 4. 数据流分析引擎 — `analysis/dataflow.py`

数据流分析基于 **IR 树遍历**，直接遍历解析器生成的 `IRNode` 树，而非依赖控制流图。

### 未初始化变量检测

通过遍历 IR 树，跟踪已定义变量集合。当遇到变量使用时，若该变量尚未出现在已定义集合中，则报告未初始化警告。

```python
def _detect_uninitialized(self, result: AnalysisResult, lines: List[str]):
    defined_vars: Set[str] = set()
    # 首先收集所有函数参数（视为已初始化）
    for node in self._walk(result.ir_root):
        if node.node_type == NodeType.PARAM_DECL and node.value:
            defined_vars.add(node.value)

    for node in self._walk(result.ir_root):
        used = self._get_used_vars(node)
        defined = self._get_defined_vars(node)
        for v in used:
            if v not in defined_vars and not is_builtin(v):
                result.add_issue(AnalysisIssue(
                    cwe_id="CWE-457",
                    cwe_name="Use of Uninitialized Variable",
                    ...
                ))
        defined_vars |= defined
```

### 死赋值检测

按函数作用域遍历 IR 树，收集函数内所有赋值节点。对于每个赋值，检查其变量名是否在后续赋值节点中被使用，以及是否在整个源码中出现。保守策略优先避免误报。

```python
def _detect_dead_assignments(self, result, lines):
    for func_node in self._walk(result.ir_root):
        if func_node.node_type != NodeType.FUNCTION:
            continue

        assign_nodes = [
            n for n in self._walk(func_node)
            if n.node_type == NodeType.ASSIGN and n.value
        ]

        for i, instr in enumerate(assign_nodes):
            var = extract_var_name(instr)
            # 检查后续是否有使用
            if any(var in self._get_used_vars(later)
                   for later in assign_nodes[i+1:]):
                continue
            # 同时检查全局标识符扫描
            if var in all_used:
                continue
            result.add_issue(AnalysisIssue(
                cwe_id="CWE-563",
                cwe_name="Assignment to Variable without Use",
                ...
            ))
```

### IR 树遍历

```python
def _walk(self, node: IRNode):
    """递归遍历整个 IR 树，生成所有节点"""
    if node is None:
        return
    yield node
    for child in node.children:
        yield from self._walk(child)
```

---

## 5. 污点分析引擎 — `analysis/taint_analyzer.py`

### Source-Propagation-Sink 模型

```
污点源 (Source)           传播规则                   污点汇 (Sink)
─────────────────         ──────────────────          ────────────────
scanf(buffer)   →  标记buffer为污点  →  system(buffer)   → CWE-78
input()         →  标记var为污点    →  cursor.execute(var)→ CWE-89
getParameter()  →  标记param为污点  →  Runtime.exec()    → CWE-78
```

实现核心：

```python
# 预设污点源（按语言分类）
TAINT_SOURCES = {
    "Python": {
        "input":       ("CWE-20", "Improper Input Validation"),
        "request.GET": ("CWE-20", "Improper Input Validation"),
        "sys.argv":    ("CWE-20", "Improper Input Validation"),
    },
    "C": {
        "scanf": ("CWE-20", "Improper Input Validation"),
        "gets":  ("CWE-120", "Buffer Copy without Checking Size"),
    },
    ...
}

# 预设污点汇（按漏洞类型分类）
TAINT_SINKS = {
    "command_injection": {
        "functions": ["system", "exec", "subprocess.run", "os.system"],
        "cwe_id": "CWE-78",
    },
    "sql_injection": {
        "functions": ["execute", "cursor.execute", "executeQuery"],
        "cwe_id": "CWE-89",
    },
}

def _detect_taint_sources(self, lines, result):
    for i, line in enumerate(lines, 1):
        for src in self.taint_sources:
            if src in line:
                m = re.match(r'\s*(\w+)\s*=', line)
                if m:
                    self.tainted_vars.add(m.group(1))  # 标记为污点变量

def _detect_taint_sinks(self, lines, result):
    for i, line in enumerate(lines, 1):
        for category, sink_info in TAINT_SINKS.items():
            for func in sink_info["functions"]:
                if func in line:
                    # 检查污点变量是否流入该 Sink
                    if any(tv in line for tv in self.tainted_vars):
                        result.add_issue(AnalysisIssue(
                            cwe_id=sink_info["cwe_id"], ...
                        ))
```

---

## 6. 模式检测器 — `analysis/pattern_detector.py`

### 多层检测策略

```python
class PatternDetector:
    def detect(self, result):
        if language in (C, CPP):  self._detect_c_cpp(lines, result)
        if language == JAVA:      self._detect_java(lines, result)
        if language == PYTHON:    self._detect_python(lines, result)
        self._detect_common(lines, result)   # 跨语言通用检测
```

### 典型检测模式

| 检测项 | 技术手段 | CWE |
|--------|---------|-----|
| 空 catch 块 | 状态机（进入catch → 检查内容） | CWE-390 |
| 硬编码凭证 | 正则 `password\s*=\s*["'][^"']{3,}["']` | CWE-798 |
| 弱加密算法 | 关键词匹配 MD5/SHA1/DES/RC4 | CWE-327 |
| 魔术数字 | 正则排除0/1/2/10/100后的数字字面量 | CWE-398 |
| 函数过长 | 统计函数起止行，>50行报警 | CWE-1121 |
| 可变默认参数 | 正则 `def.*=\s*[\[\{]` | CWE-1188 |
| 双重释放 | 跟踪 free/malloc 调用对 | CWE-415 |

---

## 7. GUI 架构 — `gui/`

### 组件层次

```
StaticAnalyzerApp (主控制器)
├── Toolbar（工具栏）
│   ├── 文件操作按钮
│   ├── 语言选择下拉框
│   └── 统计标签（🔴/🟡/🛡）
├── CodeViewer（左侧代码编辑器）
│   ├── 行号 Text 组件
│   ├── 代码 ScrolledText（支持高亮）
│   └── 错误行 tag_configure（red/yellow/blue 背景）
├── Notebook（右侧标签页）
│   ├── IssueTreeView（问题树，支持排序/筛选）
│   ├── Fix Tab（ScrolledText 修复报告）
│   └── Taint Tab（污点分析摘要）
├── DetailPanel（底部详情面板）
└── StatusBar（状态栏 + ProgressBar）
```

### 线程安全的分析调度

分析在后台线程执行，通过 `root.after()` 回调更新 UI，避免界面卡顿：

```python
def _run_analysis(self):
    self.progress.start(10)     # 启动进度条动画
    
    def run():
        result = engine.analyze(code)
        # 通过 after() 切回主线程更新 UI（线程安全）
        self.root.after(0, lambda: self._display_results(result))
    
    threading.Thread(target=run, daemon=True).start()
```

---

## 8. CWE 漏洞覆盖范围

| CWE ID | 名称 | 检测方法 |
|--------|------|---------|
| CWE-20 | Improper Input Validation | 污点源识别 |
| CWE-22 | Path Traversal | 污点 → open() Sink |
| CWE-78 | OS Command Injection | 污点 → system/exec Sink |
| CWE-79 | XSS | 污点 → innerHTML Sink |
| CWE-89 | SQL Injection | 污点 → execute() Sink |
| CWE-95 | Code Injection | eval() 使用检测 |
| CWE-120 | Buffer Overflow | 危险函数 gets/strcpy 检测 |
| CWE-125 | Out-of-bounds Read | memcpy 无边界检测 |
| CWE-134 | Format String | printf(buffer) 模式 |
| CWE-190 | Integer Overflow | 类型+值范围检测 |
| CWE-327 | Weak Cryptography | MD5/SHA1/DES 关键词 |
| CWE-330 | Insufficient Randomness | Math.random 检测 |
| CWE-390 | Exception Swallowing | 空catch块状态机 |
| CWE-398 | Poor Code Quality | 代码风格/命名规范问题 |
| CWE-415 | Double Free | free() 调用跟踪 |
| CWE-457 | Uninitialized Variable | IR 树遍历分析 |
| CWE-476 | NULL Pointer Dereference | malloc无检查 + Java NPE |
| CWE-563 | Dead Assignment | IR 树作用域分析 |
| CWE-597 | Wrong String Comparison | Java `==` 检测 |
| CWE-691 | Control Flow Issue | 括号匹配算法 |
| CWE-772 | Resource Leak | open/fopen 跟踪 |
| CWE-798 | Hardcoded Credentials | 正则密码/密钥模式 |
| CWE-807 | Untrusted Input Reliance | 环境变量污点源 |
| CWE-1121 | Excessive Complexity | 函数行数统计 |
| CWE-1188 | Insecure Default Init | 可变默认参数检测 |
