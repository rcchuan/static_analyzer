# MultiLang Static Code Analyzer

> 多语言静态代码分析器 — 支持 C / C++ / Java / Python

一款具备图形化界面的静态代码安全分析工具，集成了控制流分析、数据流分析、污点追踪、程序切片和 CWE 漏洞检测能力。

---

## 运行使用说明

### 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.8 及以上 |
| 操作系统 | Windows 10+ / macOS 10.15+ / Linux (有桌面环境) |
| 依赖库 | 仅标准库（tkinter 内置于 Python） |

---

### 快速启动

**第一步：克隆或下载项目**

\`\`\`bash
git clone https://github.com/yourname/static-analyzer.git
cd static-analyzer
\`\`\`

**第二步：直接运行**（无需安装任何第三方库）

\`\`\`bash
python main.py
\`\`\`

如果 tkinter 未安装（部分 Linux）：

\`\`\`bash
# Ubuntu / Debian
sudo apt-get install python3-tk

# Fedora / RHEL
sudo dnf install python3-tkinter

# macOS (via Homebrew)
brew install python-tk
\`\`\`

---

### 打包为可执行文件（.exe / 二进制）

**安装 PyInstaller：**

\`\`\`bash
pip install pyinstaller
\`\`\`

**一键打包脚本：**

\`\`\`bash
python build.py
\`\`\`

打包成功后，可执行文件位于：
- Windows：`dist/StaticAnalyzer.exe`
- Linux/macOS：`dist/StaticAnalyzer`

**手动打包命令：**

\`\`\`bash
pyinstaller --onefile --windowed --name StaticAnalyzer \
  --add-data "core:core" \
  --add-data "parsers:parsers" \
  --add-data "analysis:analysis" \
  --add-data "gui:gui" \
  main.py
\`\`\`

> Windows 使用分号分隔路径：`--add-data "core;core"`

---

## 界面使用说明

### 主界面布局

\`\`\`
┌─────────────────────────────────────────────────────────────┐
│  工具栏：打开文件 | 开始分析 | 程序切片 | 导出报告 | 清空  │
├──────────────────────┬──────────────────────────────────────┤
│                      │  ┌──────────────────────────────────┐ │
│   📝 源代码编辑器    │  │ ⚠问题列表 | 🔀CFG | 🛠修复 | ☣污点│ │
│                      │  ├──────────────────────────────────┤ │
│  • 行号显示          │  │  问题树形列表（可筛选/搜索）     │ │
│  • 错误行高亮        │  │  每行：行号/严重性/类别/CWE     │ │
│  • 点击跳转          │  ├──────────────────────────────────┤ │
│                      │  │  📋 问题详情面板                 │ │
│                      │  │  标题/CWE/描述/代码/修复建议    │ │
└──────────────────────┴──────────────────────────────────────┘
│  状态栏：分析语言 | 问题数量 | 代码行数 | 基本块数          │
└─────────────────────────────────────────────────────────────┘
\`\`\`

---

### 功能操作指南

#### 1. 打开并分析文件

1. 点击工具栏 **📂 打开文件**
2. 选择 `.py` / `.c` / `.cpp` / `.java` 文件
3. 工具自动检测语言（也可手动在右侧下拉框选择）
4. 点击 **▶ 开始分析**
5. 等待分析完成（进度条显示中）

> **也可直接在左侧代码编辑器中粘贴代码**，然后点击"开始分析"。

#### 2. 查看问题列表（⚠ 问题列表 标签页）

- 顶部勾选框可按严重性筛选：🔴错误 / 🟡警告 / 🔵提示
- 搜索框支持按标题、描述、行号关键词过滤
- **点击任意一行**：
  - 左侧代码编辑器自动跳转并高亮该行
  - 底部详情面板显示完整的 CWE 编号、描述、修复建议

#### 3. 查看控制流图（🔀 控制流图 标签页）

- 自动绘制基本块 (BB) 和控制流边
- 节点颜色含义：
  - 🟢 绿色 = ENTRY（程序入口）
  - 🔴 红色 = EXIT（程序出口）
  - 🔵 蓝色边框 = 循环块
  - 灰色 = 死代码块
- 边的颜色：绿色=true分支 / 红色=false分支 / 黄色=异常 / 蓝色=回边
- 可拖拽和滚轮缩放

#### 4. 查看修复方案（🛠 修复方案 标签页）

- 按问题类别分组展示所有修复建议
- 每个问题包含：CWE 编号 + 完整描述 + 具体修复步骤
- 底部汇总安全加固建议和参考链接

#### 5. 污点分析视图（☣ 污点分析 标签页）

- 展示所有检测到的污点漏洞（Source → Sink 路径）
- 显示预设的污点源和检测点规则

#### 6. 程序切片（🔪 程序切片）

1. 分析完成后点击工具栏 **🔪 程序切片**
2. 输入切片准则：
   - **行号**：感兴趣的代码行（例如 `15`）
   - **变量名**：该行中要追踪的变量（例如 `cmd`）
3. 点击"计算切片"，弹出窗口展示所有影响该变量在该行取值的语句

#### 切片功能的实际意义

| 场景             | 用法                                                   |
| ---------------- | ------------------------------------------------------ |
| 追踪注入漏洞来源 | 以 `os.system(x)` 所在行+变量名为准则，回溯 x 从哪里来 |
| 定位崩溃根因     | 以崩溃行+出问题的变量为准则，找到所有赋值路径          |
| 理解复杂赋值链   | 多次转赋值（a→b→c→d）时，切片一次看清完整链路          |

> **注意**：行号是源代码中的实际行号（编辑器左侧行号栏显示的数字）。变量名必须和代码中写的完全一致，区分大小写。

#### 7. 导出报告

- 点击 **💾 导出报告** 将分析报告保存为 `.txt` 文件
- 报告包含所有问题的分类、CWE 编号、修复建议和安全参考链接

---

## 语言支持范围

| 分析能力 | Python | C | C++ | Java |
|---------|:------:|:-:|:---:|:----:|
| 语法错误检测 | ✅ (AST) | ✅ | ✅ | ✅ |
| 控制流图 (CFG) | ✅ | ✅ | ✅ | ✅ |
| 可到达定义分析 | ✅ | ✅ | ✅ | ✅ |
| 活性分析/死代码 | ✅ | ✅ | ✅ | ✅ |
| 静态后向切片 | ✅ | ✅ | ✅ | ✅ |
| 污点分析 | ✅ | ✅ | ✅ | ✅ |
| 缓冲区溢出检测 | — | ✅ | ✅ | — |
| 空指针检测 | — | ✅ | ✅ | ✅ |
| 硬编码凭证 | ✅ | ✅ | ✅ | ✅ |
| 弱加密算法 | ✅ | ✅ | ✅ | ✅ |
| 资源泄漏 | ✅ | ✅ | ✅ | ✅ |

---

## 项目结构

\`\`\`
static_analyzer/
├── main.py                    # 程序入口
├── build.py                   # 打包脚本
├── requirements.txt
├── core/
│   └── ir.py                  # 中间表示层（IR）
├── parsers/
│   ├── python_parser.py       # Python AST 解析器
│   └── generic_parser.py      # C/C++/Java 正则解析器
├── analysis/
│   ├── engine.py              # 分析引擎（流程编排）
│   ├── cfg_builder.py         # 控制流图构建
│   ├── dataflow.py            # 数据流分析引擎
│   ├── taint_analyzer.py      # 污点分析引擎
│   ├── pattern_detector.py    # 模式检测器
│   ├── slicer.py              # 程序切片
│   └── fix_generator.py       # 修复报告生成器
├── gui/
│   ├── app.py                 # 主界面
│   └── widgets.py             # 自定义控件
└── samples/
    ├── vulnerable_python.py   # Python 漏洞示例
    ├── vulnerable_c.c         # C 漏洞示例
    └── vulnerable_java.java   # Java 漏洞示例
\`\`\`
